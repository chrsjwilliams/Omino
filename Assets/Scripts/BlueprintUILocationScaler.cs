
using UnityEngine;

public class BlueprintUILocationScaler : MonoBehaviour {

	// Use this for initialization
	void Awake () {
        //transform.localScale = 
        //    new Vector3(((float)Screen.width / (float)Screen.height) 
        //    / (3f / 4), 
        //    1, 1);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
